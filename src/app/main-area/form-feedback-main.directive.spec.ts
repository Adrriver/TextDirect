import { FormFeedbackMainDirective } from './form-feedback-main.directive';

describe('FormFeedbackMainDirective', () => {
  it('should create an instance', () => {
    const directive = FormFeedbackMainDirective;
    expect(directive).toBeTruthy();
  });
});
